[project]
name = "mcp-server"
version = "0.1.0"
description = "FastMCP server for TensorZero integration with example tools"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.10.6",
    "starlette>=0.46.2",
    "uvicorn>=0.34.3",
]
