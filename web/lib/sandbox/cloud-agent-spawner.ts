/**
 * Compat shim — tests and some legacy callers import from this path.
 *
 * The real implementation lives in `lib/sandbox/spawn/cloud-agent-spawner.ts`.
 *
 * @see lib/sandbox/spawn/cloud-agent-spawner.ts
 */

export * from '@/lib/sandbox/spawn/cloud-agent-spawner';
