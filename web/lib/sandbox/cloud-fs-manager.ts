/**
 * Compat shim — tests and some legacy callers import from this path.
 *
 * The real implementation lives in `lib/virtual-filesystem/cloud-fs-manager.ts`.
 *
 * @see lib/virtual-filesystem/cloud-fs-manager.ts
 */

export * from '@/lib/virtual-filesystem/cloud-fs-manager';
