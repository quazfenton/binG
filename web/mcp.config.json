{
  "$schema": "https://json.schemastore.org/mcp.settings.json",
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "enabled": false,
      "disabledReason": "stdio/npx server — only available in desktop mode",
      "description": "Live documentation + framework context injection (disabled — stdio only)"
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "enabled": false,
      "disabledReason": "stdio/npx server — only available in desktop mode",
      "description": "Read/write local files with project root access (disabled — stdio only)"
    },
    "git": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/mcp-server-git", "."],
      "enabled": false,
      "disabledReason": "Package @modelcontextprotocol/mcp-server-git not found on npm registry",
      "description": "Git repository analysis and operations (disabled — package unavailable)"
    },
    "fetch": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/mcp-server-fetch"],
      "enabled": false,
      "disabledReason": "Package @modelcontextprotocol/mcp-server-fetch not found on npm registry",
      "description": "HTTP requests and API fetching (disabled — package unavailable)"
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "enabled": false,
      "disabledReason": "stdio/npx server — only available in desktop mode",
      "description": "Persistent memory storage across sessions (disabled — stdio only)"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "enabled": false,
      "disabledReason": "stdio/npx server — only available in desktop mode",
      "description": "Structured reasoning chains for complex tasks (disabled — stdio only)"
    },
    "sqlite": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sqlite", "./data/app.db"],
      "enabled": false,
      "description": "Local SQLite database querying (optional)"
    },
    "puppeteer": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-puppeteer"],
      "enabled": false,
      "description": "Headless browser automation (optional)"
    },
    "bash": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-bash"],
      "enabled": false,
      "description": "Run shell commands with safety controls (use with caution)"
    }
  },
  "mcpSettings": {
    "autoConnect": true,
    "connectionTimeout": 30000,
    "toolCallTimeout": 60000,
    "logging": {
      "level": "info",
      "file": "./logs/mcp.log"
    },
    "security": {
      "allowFileSystemAccess": true,
      "allowedPaths": ["./", "./app/", "./lib/", "./components/"],
      "blockedCommands": ["rm -rf", "mkfs", "dd", "chmod -R"],
      "requireApprovalFor": ["bash", "puppeteer"]
    }
  }
}
