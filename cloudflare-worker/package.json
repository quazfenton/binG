{
  "name": "ai-orchestrator-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for AI orchestration with Fast-Agent and n8n",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "test": "vitest"
  },
  "devDependencies": {
    "wrangler": "^3.80.0",
    "vitest": "^1.0.0"
  }
}
