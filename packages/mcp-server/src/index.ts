/**
 * @bing/mcp-server
 *
 * Standalone MCP server package.
 * Re-exports the shared stdio server implementation.
 */

/**
 * binG MCP Server — Library exports
 *
 * Re-exports the stdio server instance and configuration for programmatic use.
 */

export { server, config } from './stdio-server.js';
export * from './stdio-server.js';
