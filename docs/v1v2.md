yes but there is a discrepancy. 

 1. a lot of the other orchestration modes can actually be used with regular API calls using SDKs like OpenAI responses, engineered full within our app (which is what the term v1 is intended to mean)  and 

2. a lot of them are using the 'prebuilt' engines of CLI binaries or spawns which may be remote
(this architectural difference in how the app needs to handle this is what v2 is intended to mean, requiring smaller amounts manual engineering such as a. piping of their outputs to fit with our app's web version, 

or b. more slightly native handling for desktop version since these programs like Opencode CLI can already handle local files as opposed to v1's needed text parsing for virtual file edits; 
c. same thing as b may be able if i potentially use a certain type of web implementation style with docker compose containers where v2 modes may possibly run as spawns in containers and possibly have containerized workspaces with direct file edits,
 or in-memory or some other implementation piped back to the web database, or another type of file storage/database if any other filesystem implementation is better for this.).

3. 
a. a lot of the orchestration modes may be used with both architectures, but are incorrectly distinguished outside of this framing as if they are separate from the explicitly named 'v1' or 'v2' modes but the term v1 and 2 are only meant to distinguish the above differences in handling. 

b. The Exception is some modes that are explicitly only v2 because their core implementation explicitly imports the services that are based on using spawn and exec (though The safest alternative to both is to use execFile ) such as opencode-cli.ts or whatever CLI program. 
Most other impelemtations are or should be abstracted enough to be able to use both v1 (different api calls or threads that can engineered someway such as engineering their memory or manual reinforcement roles etc.)  or multi-agent spawns of v2 (which can also be engineered but technically require less manual enforcement. Ie. things like engineering their memory aren't as required since the program's instance handles context and memory etc., but 

c. these things can also be further engineered for the v2 architectures by spawning new agents for for different roles or using any of the cli's arguments for whatever such as new session argument for blank state of memory for , skills argument for injecting powers, or other arguments for compaction or the program internally having the use of subagents etc.  Currently none of this is implemented for advanced engineering or per-program CLI input format.
Currently all the context/memory/other implementations seem to be v1, not abstracted in this way to fit v2 usages but there are separate implemetations of files for workers and remote spawn etc. that may fit this. 
Versions of a v2 CLI program engine that can be hosted remotely/localhost on desktop 
or in another v2 engine container on web etc. are somewhat akin to v1 adjacent because they can use remote calls/communication and therefore examples of CLI engine usages that can actually be used similar to v1  

d) On the other hand Modes like opencode-sdk / pi implemetation which also uses sdk , may be different in how to adapt to the v1 - native engineering since they use an SDK and may or may not programmatically implement methods or easily adapt without different instances.

  More issues 
4.  
Model selection isnt necessarily correctly setup for the v2 modes that are local such as desktop or a possible containerized web, and im not sure if model selection can be configured for the v2 modes that are remote such as remotely hosted nullclaw or Opencode/Codex etc. implementations (other than the 'remote' SDK implementations I am about to mention).  
6.
Modes like opencode-sdk  and pi implemetation which also uses sdk , can innately implement model selection programmatically since they use an SDK and can be somewhat akin to v1 / regular model selection and adapt to llm-providers.ts and can easily fit custom provider selection in UI.

7.
For  another version of a CLI program engine that uses execFile for spawn, may need different implementation for model selection, such as: 
a) local desktop v2 binary use - possibly re-exporting env variables or even better, more easily using the model CLI parameter for whatever v2 engine/program it is. 
b) if hosted remotely/localhost on desktop or in another v2 engine container on web etc. may need communication and remote file for handling to trigger the model change in the hosted environment?
or can  spawn new agent & wind down current model's agent and pass context? But the 1st is likely better, or another idea