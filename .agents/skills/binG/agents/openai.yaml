interface:
  display_name: "BinG"
  short_description: "Repo-specific patterns and workflows for binG"
  default_prompt: "Use the binG repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true