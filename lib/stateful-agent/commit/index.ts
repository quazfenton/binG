export { 
  ShadowCommitManager, 
  commitTool, 
  rollbackTool, 
  historyTool,
  generateUnifiedDiff 
} from './shadow-commit';
export type { TransactionEntry, ShadowCommitOptions, CommitResult } from './shadow-commit';
