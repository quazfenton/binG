// Client-side config. IMPORTANT: do NOT place real secret keys here in production.
// For demo only: DEFAULT_PUTER_KEY is an ephemeral, rate-limited demo key you control server-side instead.
export const DEFAULT_PUTER_KEY = '' // leave empty for production; prefer server proxy
export const PUTER_PROXY_BASE = '/api/puter/chat' // points to the server proxy in production