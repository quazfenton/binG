# pnpm configuration
# Skip optional dependencies to avoid resolution errors
optional=true

# Use maximum available concurrency
node-linker=hoisted

# Don't enforce strict peer dependencies
strict-peer-dependencies=false

# Allow hoisting all dependencies
shamefully-hoisted=true

# Ignore scripts during install (faster)
ignore-scripts=true
